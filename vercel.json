{
  "buildCommand": "npm run build --workspace=frontend && ls -la packages/frontend/ && ls -la packages/frontend/dist/",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "npm install --include=dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
