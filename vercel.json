{
  "buildCommand": "npm run build --workspace=frontend && cp -r packages/frontend/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "npm install --include=dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
