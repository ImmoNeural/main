{
  "buildCommand": "npm run build --workspace=frontend && pwd && ls -la && find . -name 'index.html' -type f",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "npm install --include=dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
