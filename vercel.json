{
  "buildCommand": "npm install --prefix packages/frontend && npm run build --prefix packages/frontend",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
